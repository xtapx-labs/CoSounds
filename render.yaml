databases:
  - name: cosounds-db
    plan: free
    databaseName: cosounds
    user: cosounds

services:
  - type: web
    plan: free
    name: cosounds
    runtime: python
    buildCommand: './build_prod_server.sh'
    startCommand: './start_prod_server.sh'
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: cosounds-db
          property: connectionString
      - key: SECRET_KEY
        generateValue: true
      - key: WEB_CONCURRENCY
        value: 4
      - key: PYTHON_VERSION
        value: 3.13.1
      # Add your AWS credentials as secrets in Render Dashboard
      - key: AWS_ACCESS_KEY_ID
        sync: false
      - key: AWS_SECRET_ACCESS_KEY
        sync: false
      - key: AWS_STORAGE_BUCKET_NAME
        sync: false
      - key: AWS_S3_REGION_NAME
        sync: false
      # Django Debug Toggle
      - key: DEBUG
        value: false
